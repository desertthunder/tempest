# Parking Lot (TODO)

- Improve external admin DID login UX: when `TEMPEST_ADMIN_DID` is not hosted locally, `/admin/login` currently asks for a pasted OAuth access token. Add a browser-driven OAuth authorization flow so external admins can sign in without manually obtaining a token.
